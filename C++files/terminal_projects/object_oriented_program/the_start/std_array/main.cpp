#include <iostream>
#include <array>
#include <vector>

using namespace std;

int main()
{
    std::array<int, 5> arr = {1, 2, 3, 4, 5}; // std::array<type, size>

    for (int i = 0; i < array.size(); i++)
    {
        cout << array[i] << " ";
    }
    return 0;
}
